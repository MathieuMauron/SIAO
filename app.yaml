runtime: php82
env: standard
entrypoint: serve public/index.php

runtime_config:
  document_root: public

handlers:
  - url: "/favicon\\.ico"
    static_files: public/favicon.ico
    upload: public/favicon\\.ico

  - url: "/(.+\\.(css|js|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot))"
    static_files: public/\1
    upload: public/(.+\.(css|js|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot))

  - url: /.*
    script: auto

beta_settings:
  cloud_sql_instances: siao-456206:europe-west1:bdd-siao

env_variables:
  APP_ENV: production
  DB_CONNECTION: mysql
  DB_HOST: /cloudsql/siao-456206:europe-west1:bdd-siao
  DB_PORT: 3306
  DB_DATABASE: siao
  DB_USERNAME: siao
  DB_PASSWORD: tototititutu83!
  LOG_CHANNEL: stack
  LOG_LEVEL: debug
  LOG_STORAGE_PATH: /tmp
  VIEW_COMPILED_PATH: /tmp
